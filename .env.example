# MongoDB connection string
MONGODB_URI=mongodb://127.0.0.1:27017/shortlist

# Public site URL (no trailing slash) — used for canonical, OG, sitemap
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Admin login (pre-prod credentials shown on login form by design)
ADMIN_USERNAME=adnan.akhonbay@gmail.com
ADMIN_PASSWORD=interviewx@1234

# Session / JWT signing secret (use a long random string in production)
JWT_SECRET=change-me-to-a-long-random-secret
SESSION_SECRET=change-me-to-a-long-random-secret
